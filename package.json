{
  "name": "sheep-and-wolves",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "predev": "npm run build -w packages/shared",
    "dev": "concurrently -n shared,web -c yellow,magenta \"npm run watch -w packages/shared\" \"npm run dev -w apps/web\"",
    "dev:web": "npm run build -w packages/shared && npm run dev -w apps/web",
    "dev:functions": "supabase functions serve",
    "build:shared": "npm run build -w packages/shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
