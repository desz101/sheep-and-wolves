PORT=4000
CLIENT_ORIGIN=http://localhost:3000

# https://supabase.com/dashboard/project/frdjrmowinwbhglatufa/settings/api
SUPABASE_URL=https://frdjrmowinwbhglatufa.supabase.co

# Project Settings -> API -> "service_role" secret key (NOT the publishable/anon
# key -- that one is meant for browsers and has zero access under this project's
# RLS setup; see supabase/schema.sql). Never commit the real value or expose it
# to the client -- it bypasses Row Level Security entirely.
SUPABASE_SERVICE_ROLE_KEY=
